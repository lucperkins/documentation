machine:
  node:
    version: 8.1.0
  timezone:
    America/Los_Angeles
dependencies:
  cache:
    cache_directories:
      - ~/.cache/yarn
  override:
    - make circleci-setup
test:
  - make build
deployment:
  production:
    branch: lperkins/boilerplate
    commands:
      - make deploy
