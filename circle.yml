machine:
  node:
    version: 8.1.0
  timezone: America/Los_Angeles
dependencies:
  pre:
    - yarn install
  cache:
    cache_directories:
      - cache_directories
  override:
    - make circleci-setup
test: make build
deployment:
  production:
    branch: master
    commands:
      - make deploy
